@using Microsoft.Ajax.Utilities
@using WebApplication3.Models
<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("MineShop", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li>@Html.ActionLink("Przedmioty", "Index", "Przedmiots")</li>
                @if (User.IsInRole(RoleName.CanManagePredmiot))
                {

                    <li>@Html.ActionLink("Pruducenci", "Index", "Producents")</li>
                    <li>@Html.ActionLink("Kategorie", "Index", "Kategorias")</li>

                }
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>